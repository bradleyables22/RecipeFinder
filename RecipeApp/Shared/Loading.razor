@inject IJSRuntime _js
<div id="circle" class="bg-primary" style="position: absolute;left:0;top:-20px; height:20px;width:400px;"></div>

@code{
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        await base.OnAfterRenderAsync(firstRender);
        await _js.InvokeVoidAsync("moveCircle","#circle");

    }

}

